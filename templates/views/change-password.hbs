
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Change Password</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f4;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .container {
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
      text-align: center;
      max-width: 400px; /* Adjusted max-width for responsiveness */
      width: 100%;
    }

    h2 {
      color: #007bff;
      margin-bottom: 20px;
      font-weight: bold;
    }

    .form-group {
      text-align: left;
      margin-bottom: 20px;
    }

    input {
      width: 100%;
      padding: 10px;
      box-sizing: border-box;
      margin-top: 5px;
    }

    #resetBtn {
      background-color: #007bff;
      color: #fff;
      padding: 10px;
      cursor: pointer;
      border: none;
      border-radius: 5px;
      width: 100%;
    }

    #errorMessage {
      color: red;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Change Password</h2>

    <form id="changePasswordForm" action="/change-password" method="POST">
      <!-- Display phone number as read-only -->
      <div class="form-group">
        <label for="phoneNumber">Phone Number:</label>
        <input type="text" id="phoneNumber" name="phoneNumber" value="{{phoneNumber}}" readonly class="form-control">
      </div>

      <!-- New Password -->
      <div class="form-group">
        <label for="newPassword">New Password:</label>
        <input type="password" id="newPassword" name="newPassword" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*\W)(?!.*\s).{8,}" 
          title="Password must have at least 8 characters, 1 digit, 1 uppercase letter, 1 lowercase letter, and 1 special character" required
          class="form-control">
      </div>

      <!-- Confirm New Password -->
      <div class="form-group">
        <label for="confirmPassword">Confirm New Password:</label>
        <input type="password" id="confirmPassword" name="confirmPassword" required class="form-control">
      </div>

      <!-- Error Message -->
      <div id="errorMessage"></div>

      <!-- Reset Button -->
      <button type="submit" id="resetBtn" class="btn btn-primary">Reset Password</button>
    </form>
  </div>

  <!-- jQuery for adding custom validation -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script>
    $(document).ready(function () {
      $('#changePasswordForm').submit(function (e) {
        var newPassword = $('#newPassword').val();
        var confirmPassword = $('#confirmPassword').val();

        if (newPassword !== confirmPassword) {
          $('#errorMessage').html('<p style="color: red;">Passwords do not match. Please try again.</p>');
          e.preventDefault(); // Prevent form submission
        }
      });
    });
  </script>

  <!-- Bootstrap JS -->
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
