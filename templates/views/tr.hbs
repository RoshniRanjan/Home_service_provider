<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Providers Directory</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="styles.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa; /* Light Gray Background */
            color: #343a40; /* Dark Gray Text Color */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        h2 {
            color: #007bff; /* Blue Heading Color */
        }

        .card {
            margin-bottom: 20px;
            border: none;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            cursor: pointer;
            background-color: #f8f9fa; /* Light Gray Background */
            border: 1px solid #dee2e6; /* Light Gray Border */
        }

        .card-header:hover {
            background-color: #e2e6ea; /* Light Blue Background on Hover */
        }

        .profile-image {
            width: 100%;
            max-height: 150px;
            object-fit: cover;
            border-radius: 8px;
            cursor: pointer;
        }

        .card-body {
            border-top: 1px solid #dee2e6; /* Light Gray Border */
            padding: 20px;
        }

        .call-button {
            background-color: #28a745; /* Green Button Color */
            color: #fff; /* White Text Color */
            border: none;
            padding: 8px 16px;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .call-button:hover {
            background-color: #218838; /* Darker Green on Hover */
        }

       
    </style>
</head>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="script.js"></script>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<script>
    function showLargeImage(imageSrc) {
        var modalImage = document.querySelector('#largeImageModal img');
        modalImage.src = imageSrc;
        $('#largeImageModal').modal('show');
    }
    $(document).ready(function () {
        $('#largeImageModal').on('click', function (e) {
            if ($(e.target).hasClass('modal fade')) {
                $('#largeImageModal').modal('hide'); // Close modal if click occurs outside modal content
            }
        });
    });

        

</script>



<body>
    

    <div class="container mt-5">
        <h2 class="text-center mb-4">Service Providers Directory</h2>

        <ul>
            {{#each providerdata}}
            <div id="accordion" class="mt-4">
                <!-- Service Providers List -->
                <div class="card" data-services={{services}}>
                    <div class="card-header d-flex align-items-center">
                        <!-- Profile Image -->
                        <div class="col-md-3 col-sm-12 mb-3 col-lg-3">
                            <img src={{profileImage}} alt="Profile Image" class="profile-image"
                                onclick="showLargeImage('{{profileImage}}')">
                        </div>
                        <!-- Business Name and Ratings -->
                        <div class="col-md-9 col-sm-12 mb-3 col-lg-9">
                            <h4 class="mb-0">{{fullName}}</h4>
                        </div>
                    </div>
                    <div class="card-body">
                        <p><strong>Address:</strong> {{Address}}</p>
                        <p><strong>Contact:</strong> {{phoneNumber}}</p>
                        <p><strong>Hours of Working:</strong> Mon-Fri: {{preferredTiming}}</p>
                        <p><strong>City: </strong>{{city}}</p>
                        <p><strong>Services:</strong>
                            {{#each services}}
                            {{this}}{{#unless @last}}, {{/unless}}
                            {{/each}}
                        </p>
                      <a class="btn btn-success" href="https://wa.me/{{phoneNumber}}" target="_blank">
        <i class="fab fa-whatsapp mr-2"></i>Open WhatsApp
    </a>

                    </div>
                </div>
            </div>
            {{/each}}
        </ul>
    </div>

    <!-- Enlarged Image Modal -->
    <!-- Enlarged Image Modal -->
<div class="modal fade" id="largeImageModal" tabindex="-1" role="dialog" aria-labelledby="largeImageModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-xl" role="document"> <!-- Use modal-xl for extra-large modal -->
        <div class="modal-content">
            <div class="modal-body text-center">
                <img src="" alt="Enlarged Profile Image" class="img-fluid" style="max-width: 400px; max-height: 400px;"> <!-- Use img-fluid for responsive image -->
            </div>
        </div>
    </div>
</div>

    {{>footer}}
</body>

</html>
